{
  "name": "@makstashkevich/zustand-model",
  "version": "1.0.5",
  "description": "A lightweight and powerful Zustand plugin for simplified asynchronous API state management in React. Eliminates boilerplate for loading, error, and data handling, offering a clean, provider-less solution with built-in caching.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "push": "npm run build && npm publish --access public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "zustand",
    "model",
    "state-management",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "zustand": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "zustand": ">=4.0.0"
  }
}